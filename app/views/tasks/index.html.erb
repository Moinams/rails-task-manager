<h1>Voici la liste des Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
    <%= link_to task.title, task %>
      <%= task.title %>
      <%= task.details %>
      <%= task.completed %>
    </li>
  <% end %>
</ul>
